# frozen_string_literal: true

RSpec.describe Rsstsvop do
  it 'has a version number' do
    expect(Rsstsvop::VERSION).not_to be_nil
  end

  it 'does something useful' do
    ret = true
    expect(ret).to be(true)
  end
end
